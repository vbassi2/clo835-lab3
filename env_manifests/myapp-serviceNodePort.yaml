apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: lab3
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30002
  selector:
    purpose: demonstrate-envars